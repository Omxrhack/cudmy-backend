/** Token de inyección del ClientProxy hacia el microservicio auth. */
export const AUTH_SERVICE = 'AUTH_SERVICE';

/** Token de inyección del ClientProxy hacia el microservicio de verificación. */
export const VERIFICATION_SERVICE = 'VERIFICATION_SERVICE';

/** ClientProxy NATS usado para emitir eventos entre servicios. */
export const NATS_EVENTS_CLIENT = 'NATS_EVENTS_CLIENT';
